@model IEnumerable<AcTimer.Models.Category>

@{
    ViewBag.Title = "Dashboard";
}

<h2>Dashboard</h2>

<div class="container">
    <div class="row">
        <div class="col-lg-12"><a type="button" class="btn btn-primary btn-lg btn-block" href="@Url.Action("New")">Add a new category</a></div>
        <div class="col-lg-12">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th scope="col"></th>
                        <th scope="col">Category</th>
                        <th scope="col">Added Date</th>
                        <th scope="col">Added by</th>
                        <th scope="col">Edit</th>
                        <th scope="col">Delete</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@Html.ActionLink("View", "Details", routeValues: new { @id = item.Id })</td>
                            <td><strong>@item.Name</strong></td>
                            <td>@Html.DisplayFor(c => item.DateAdded)</td>
                            <td>@item.ApplicationUser.UserName</td>
                            <td>@Html.ActionLink("Edit", "Edit", routeValues: new { @id = item.Id })</td>
                            <td>@Html.ActionLink("Delete", "Delete", routeValues: new { @id = item.Id }, htmlAttributes: new { @onclick = "return confirm('Are sure wants to delete?');" })</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

